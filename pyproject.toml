[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"

[project]
name = "youtube-sentiments-analysis"
version = "0.1.0"
description = "End-to-end YouTube sentiment analysis MLOps project"
requires-python = ">=3.11,<3.12"

[tool.setuptools.packages.find]
where = ["src"]